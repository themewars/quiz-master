<div class="exam-preview" style="font-family: Arial, sans-serif; max-width: 800px; margin: 0 auto; padding: 20px; background: white; border: 1px solid #ddd; border-radius: 8px;">
    
    <!-- Title -->
    <div class="text-center mb-6">
        <h1 style="font-size: {{ $fontSize === 'small' ? '18px' : ($fontSize === 'large' ? '24px' : '20px') }}; font-weight: bold; color: #333; margin-bottom: 10px;">
            {{ $quiz->title }}
        </h1>
        <p style="font-size: {{ $fontSize === 'small' ? '12px' : ($fontSize === 'large' ? '16px' : '14px') }}; color: #666;">
            Date: {{ $examDate }} | Questions: {{ $totalQuestions }} | Time: {{ $timeLimit }}
        </p>
    </div>

    <!-- Instructions -->
    @if($includeInstructions)
    <div class="instructions mb-6" style="background: #f8f9fa; padding: 15px; border-left: 4px solid #007bff; border-radius: 4px;">
        <h3 style="font-size: {{ $fontSize === 'small' ? '14px' : ($fontSize === 'large' ? '18px' : '16px') }}; font-weight: bold; color: #333; margin-bottom: 10px;">
            Instructions:
        </h3>
        <ul style="margin: 0; padding-left: 20px; font-size: {{ $fontSize === 'small' ? '11px' : ($fontSize === 'large' ? '15px' : '13px') }}; color: #555;">
            <li>Read all questions carefully before answering.</li>
            <li>Answer all questions.</li>
            <li>Use black or blue ink only.</li>
            <li>No calculators or electronic devices allowed.</li>
            <li>Time limit: {{ $timeLimit }}</li>
        </ul>
    </div>
    @endif

    <!-- Questions -->
    <div class="questions">
        @foreach($questions as $index => $question)
        <div class="question mb-6" style="margin-bottom: 30px;">
            <div class="question-text mb-3">
                <h4 style="font-size: {{ $fontSize === 'small' ? '13px' : ($fontSize === 'large' ? '17px' : '15px') }}; font-weight: bold; color: #333; margin-bottom: 10px;">
                    {{ $index + 1 }}. {{ $question->title }}
                </h4>
            </div>
            
            <div class="answers">
                @foreach($question->answers as $answerIndex => $answer)
                <div class="answer mb-2" style="margin-bottom: 8px; padding-left: 20px;">
                    <span style="font-size: {{ $fontSize === 'small' ? '12px' : ($fontSize === 'large' ? '16px' : '14px') }}; color: #555;">
                        {{ chr(65 + $answerIndex) }}) {{ $answer->title }}
                    </span>
                </div>
                @endforeach
            </div>
        </div>
        @endforeach
    </div>

    <!-- Answer Key -->
    @if($includeAnswerKey)
    <div class="answer-key mt-8" style="margin-top: 40px; background: #fff3cd; padding: 20px; border: 1px solid #ffeaa7; border-radius: 8px;">
        <h3 style="font-size: {{ $fontSize === 'small' ? '14px' : ($fontSize === 'large' ? '18px' : '16px') }}; font-weight: bold; color: #856404; margin-bottom: 15px;">
            Answer Key:
        </h3>
        <div class="answers-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 10px;">
            @foreach($answerKey as $questionNum => $answer)
            <div style="font-size: {{ $fontSize === 'small' ? '12px' : ($fontSize === 'large' ? '16px' : '14px') }}; color: #856404;">
                <strong>Q{{ $questionNum }}:</strong> {{ $answer }}
            </div>
            @endforeach
        </div>
    </div>
    @endif

    <!-- Footer -->
    <div class="footer mt-8 text-center" style="margin-top: 40px; padding-top: 20px; border-top: 1px solid #eee;">
        <p style="font-size: {{ $fontSize === 'small' ? '10px' : ($fontSize === 'large' ? '14px' : '12px') }}; color: #999;">
            Generated by ExamGenerator AI - {{ now()->format('d/m/Y H:i') }}
        </p>
    </div>
</div>
